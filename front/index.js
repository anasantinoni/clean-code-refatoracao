import { registerRootComponent } from 'expo';

import App from './App';

// SRP (Princípio da Responsabilidade Única): Ele tem apenas uma responsabilidade: iniciar a aplicação.

registerRootComponent(App);
